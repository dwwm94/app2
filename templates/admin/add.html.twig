{% extends 'base.html.twig' %}

{% block title %} Ajout car{% endblock %}
{% block body %}
<h1>Ajout d'une voiture</h1>

    {{ form_start(form, {'attr':{'novalidate':'novalidate'}})}}
    {{ form_errors(form)}}
    <div class="row">
        <div class="col">
            {{ form_row(form.marque, {'attr':{'placeholder':'Veuillez entrer la marque'}} )}}
        </div>
        <div class="col">
            {{ form_row(form.modele, {'attr':{'placeholder':'Veuillez entrer le modéle'}} )}}
        </div>
    </div>

    {{ form_rest(form) }}

    <button type="submit" class="btn btn-primary col-12 mt-2">Ajouter</button>

    {{ form_end(form)}}

    
{% endblock %}